.PHONY: clean All

All:
	@echo "----------Building project:[ keypad - Release ]----------"
	@cd "keypad" && "$(MAKE)" -f  "keypad.mk" && "$(MAKE)" -f  "keypad.mk" PostBuild
clean:
	@echo "----------Cleaning project:[ keypad - Release ]----------"
	@cd "keypad" && "$(MAKE)" -f  "keypad.mk" clean
